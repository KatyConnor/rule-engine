package hx.com.example.rule.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * @Author mingliang
 * @Date 2018-01-30 16:16
 */
@RestController
@RequestMapping("/ruleManage")
public class RuleManageController {

    private static final Logger LOGGER = LoggerFactory.getLogger(RuleManageController.class);

    // 查看配置规则

    // 新增一个配置规则

    // 修改配置规则

    //
}
